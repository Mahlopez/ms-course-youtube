server:
  port: 8081

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Mahlopez/ms-course-youtube.git
          search-paths: config-data
          password: ${GIT_PASSWORD}
          username: ${GIT_USER}
  security:
    user:
      name: root
      password: s3cr3t