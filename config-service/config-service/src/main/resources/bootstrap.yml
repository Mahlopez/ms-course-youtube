server:
  port: 8081

spring:
  cloud:
    config:
      server:
        git:
          url: https://github.com/Mahlopez/ms-course-youtube.git
          searchPath: config-data
          username: ${GIT_USER}
          password: ${GIT_PASSWORD}
  security:
    user:
      name: root
      password: s3cr3t